#!/usr/bin/env bash

declare -a MANAGERS
while IFS= read -r manager
do
    MANAGERS+=("${manager,,}")
done < <(grep 'Name' /usr/share/xsessions/*.desktop | awk -F"=" '{print $2}')


# An array of options to choose.
declare -a options=(
"Logout"
"Poweroff"
"Reboot"
"Suspend"
)


output(){
    ${OUTPUT} "dmlogout" "$@"
}


# Piping the above array into dmenu.
# We use "printf '%s\n'" to format the array one item to a line.
choice=$(printf '%s\n' "${options[@]}" | dmenu -i -fn "Roboto Mono Nerd Font-8" -p 'Shutdown menu:' "${@}")


# What to do when/if we choose one of the options.
case $choice in
    'Logout')
        if [[ "$(echo -e "No\nYes" | dmenu -i -fn "Roboto Mono Nerd Font-8" -p "${choice}?" "${@}" )" == "Yes" ]]; then
            for manager in "${MANAGERS[@]}"; do 
                killall "${manager}" || output "Process ${manager} was not running."
            done
        else
            output "User chose not to logout." && exit 1
        fi
        ;;
    'Reboot')
        if [[ "$(echo -e "No\nYes" | dmenu -i -fn "Roboto Mono Nerd Font-8" -p "${choice}?" "${@}" )" == "Yes" ]]; then
            ~/.local/bin/reboot.sh
        else
            output "User chose not to reboot." && exit 0
        fi
        ;;
    'Poweroff')
        if [[ "$(echo -e "No\nYes" | dmenu -i -fn "Roboto Mono Nerd Font-8" -p "${choice}?" "${@}" )" == "Yes" ]]; then
            ~/.local/bin/poweroff.sh
        else
            output "User chose not to shutdown." && exit 0
        fi
        ;;
    'Suspend')
        if [[ "$(echo -e "No\nYes" | dmenu -i -fn "Roboto Mono Nerd Font-8" -p "${choice}?" "${@}" )" == "Yes" ]]; then
            systemctl suspend
        else
            output "User chose not to suspend." && exit 0
        fi
        ;;
    *)
        exit 0
    ;;
esac
